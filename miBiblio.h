#ifndef MIBIBLIO_H_INCLUDED
#define MIBIBLIO_H_INCLUDED
/**
 *\brief Solicita 2 numeros y calcula la suma
 *\param Numeros correspondientes para la suma
 *\return El resultado de la suma
 *
 */
float suma(float a,float b);

/**
 *\brief Solicita 2 numeros y calcula la resta
 *\param Numeros correspondientes para la resta
 *\return El resultado de la resta
 *
 */
float resta(float a,float b);

/**
 *\brief Solicita 2 numeros y calcula la división
 *\param Numeros correspondientes para la división
 *\return El resultado de la división
 *
 */
float division(float a,float b);

/**
 *\brief Solicita 2 numeros y calcula la multiplicación
 *\param Numeros correspondientes para la multiplicación
 *\return El resultado de la multiplicación
 *
 */
float multiplicacion(float a,float b);

/**
 *\brief Solicita un numero y calcula su factorial
 *\param  El numero correspondiente para la factorización
 *\return El resultado de la factorización
 *
 */
float factorial(float a);

#endif // MIBIBLIO_H_INCLUDED
